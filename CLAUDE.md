# hex_gh

## Knowledge Base

- When debugging errors or investigating issues in this project, call `mcp__hexgh__recall` with the error message or topic to check for known pain points before investigating from scratch (if available)
- After solving a non-trivial bug or learning something worth preserving, call `mcp__hexgh__remember` to save the learning (if available)
