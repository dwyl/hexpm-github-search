services:
  hexgh:
    build: .
    ports:
      - "${PORT:-4001}:4000"
    volumes:
      - hexgh_data:/app/data
    env_file:
      - .env
    environment:
      - PORT=4000
    restart: unless-stopped

volumes:
  hexgh_data:
